# Code changes come with tests

A change to a file under `src/` comes with a change to a test file under `tests/` in the same diff. A pure rename, a comment change or a documentation change is exempt.
