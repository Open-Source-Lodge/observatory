# Public functions have a docstring

Each public function, method and class has a docstring. The first line says what it does, in one sentence. A name that starts with `_` is private and is exempt. Test functions are exempt.
