# No secrets in the repository

No file holds an API key, a password, a token or a private key. Read secrets from the environment or from a secret manager. An example value in documentation, such as `sk-example`, is exempt.
